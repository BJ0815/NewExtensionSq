<div class="flickr">
<!-- Main -->
    <div id="main" class="wrapper">
      <div class="container">
        <header class="major">
          <h2><%= title%></h2>
          <p></p>
        </header>
      </div>
    </div>
  <!-- Main -->
    <ul class="masonry" id="proc">
    <% results.each do |p| %>
      <li class="item">
        <h3><%= p.title %></h3>
        <%= link_to(image_tag('loading.svg',data: {"original"=>image_path(p.primary_photo_extras.url_m)}, :class => 'flickr_photo image', :border => 0), products_path(:id => p.id)) %>
      </li>
    <% end %>
    </ul>

</div>